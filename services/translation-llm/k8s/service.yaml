apiVersion: v1
kind: Service
metadata:
  name: translation-llm
spec:
  # Headless: DNS resolves to pod IPs so the api router can load-balance.
  clusterIP: None
  selector:
    app: translation-llm
  ports:
    - port: 8000
      targetPort: 8000
