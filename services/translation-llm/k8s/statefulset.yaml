# Python translation-llm
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: translation-llm
  labels:
    app: translation-llm
spec:
  serviceName: translation-llm
  replicas: 3
  selector:
    matchLabels:
      app: translation-llm
  template:
    metadata:
      labels:
        app: translation-llm
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: translation-llm
          image: registry.sXXXXXXX-at-student-dhbw-mannheim-de.users.dhbw.site/translation-llm:latest
          ports:
            - containerPort: 8000
          env:
            # Preload at startup; these are also the pinned (never-evicted) pairs
            - name: PRELOAD_PAIRS
              value: "de-en,en-de"
            # Cap loaded pipelines to fit the 3Gi limit (~1Gi base + ~200Mi/model).
            - name: MAX_MODELS
              value: "6"
            # Continue the gateway's trace; export spans to Tempo.
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://grafana-tempo.translate-platform.svc.cluster.local:4317"
            - name: OTEL_SERVICE_NAME
              value: "translation-llm"
          resources:
            requests:
              # OPUS-MT inference on CPU
              cpu: "500m"
              # Model + tokenizer in RAM is couple hundred MB per loaded pair.
              memory: "1500Mi"
            limits:
              cpu: "2000m"
              memory: "3Gi"
          volumeMounts:
            - name: hf-cache
              mountPath: /cache/huggingface
          # First model download can take ~30s on slow links.
          startupProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 60   # ~5 min budget for cold cache + first model
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            periodSeconds: 10
            failureThreshold: 2
  volumeClaimTemplates:
    - metadata:
        name: hf-cache
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
